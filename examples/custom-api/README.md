# custom-api

**Status:** Iterative

Show how to add app-specific API routes that are not only the default page/data flow.

Current repo alignment:

- the current architecture can support this,
- the docs still need a clearer extension point so users know where extra API code belongs.

Suggested snippet:

- [src/App/Api/Status.hs.md](src/App/Api/Status.hs.md)
