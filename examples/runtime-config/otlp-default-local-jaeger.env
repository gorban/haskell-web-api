# Intended as a ./.env.local body layered on top of the committed defaults.
# Turn tracing on with the default local OTLP HTTP endpoint:
# http://127.0.0.1:4318/v1/traces

APP_TITLE_PREFIX=web-api-local-jaeger

OTLP_TRACING_ENABLED=true
