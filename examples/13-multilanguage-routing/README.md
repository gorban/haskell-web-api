# 13-multilanguage-routing

**Status:** Iterative

Show the desired path-based i18n story:

- `/path` defaults to the primary language,
- `/en/path` is explicit English,
- `/es/path` is Spanish,
- generated navigation links stay language-aware automatically.

Current repo alignment:

- locale is already modeled,
- the polished path-based example and defaults still need to be documented cleanly.

Suggested snippet:

- [src/App/Routes.hs.md](src/App/Routes.hs.md)
