module HarchWeb.Markup.Regions
  ( Region,
    RegionId,
    RegionPatch,
    mkRegionId,
    region,
    regionHtml,
    regionPatchHtml,
    regionPatchId,
    replaceRegion,
  )
where

import HarchWeb.Markup.Implementation
